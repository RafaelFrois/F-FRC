{
  "version": 2,
  "crons": [
    {
      "path": "/api/score/refresh?week=1",
      "schedule": "0 12 * * *"
    }
  ],
  "routes": [
    { "handle": "filesystem" },
    { "src": "/api/(.*)", "dest": "/api/$1" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
